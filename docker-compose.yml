version: 2
services:
  pimatic:
    image: michbeck100/pimatic
    restart: always
    ports:
      - 80:80
    devices:
      - /dev/ttyUSB0:/dev/ttyUSB0
    volumes:
      - /opt/pimatic:/data
      - pimatic:/opt/pimatic
    network_mode: host
volumes:
  pimatic:
